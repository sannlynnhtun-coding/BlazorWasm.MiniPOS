@page "/"
@implements IDisposable
@inject ChartStateContainer StateContainer

<div class="space-y-6 animate-fade-in">
    <!-- KPI Cards Grid -->
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
        <!-- Total Sales Card -->
        <div class="bg-white rounded-xl border border-slate-200 p-6 shadow-sm flex items-center justify-between">
            <div>
                <p class="text-sm font-medium text-slate-500 mb-1">Total Revenue</p>
                <h3 class="text-2xl font-semibold text-slate-900">@GrandTotal.ToString("N0") <span class="text-sm font-normal text-slate-500">MMK</span></h3>
            </div>
            <div class="w-12 h-12 bg-indigo-50 text-indigo-600 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
            </div>
        </div>

        <!-- Total Products Card -->
        <div class="bg-white rounded-xl border border-slate-200 p-6 shadow-sm flex items-center justify-between">
            <div>
                <p class="text-sm font-medium text-slate-500 mb-1">Total Products</p>
                <h3 class="text-2xl font-semibold text-slate-900">@ProductCount</h3>
            </div>
            <div class="w-12 h-12 bg-blue-50 text-blue-600 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"/></svg>
            </div>
        </div>

        <!-- Categories Card -->
        <div class="bg-white rounded-xl border border-slate-200 p-6 shadow-sm flex items-center justify-between">
            <div>
                <p class="text-sm font-medium text-slate-500 mb-1">Categories</p>
                <h3 class="text-2xl font-semibold text-slate-900">@CategoryCount</h3>
            </div>
            <div class="w-12 h-12 bg-purple-50 text-purple-600 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"/></svg>
            </div>
        </div>

        <!-- Recent Sales Card -->
        <div class="bg-white rounded-xl border border-slate-200 p-6 shadow-sm flex items-center justify-between">
            <div>
                <p class="text-sm font-medium text-slate-500 mb-1">Vouchers</p>
                <h3 class="text-2xl font-semibold text-slate-900">@VoucherCount</h3>
            </div>
            <div class="w-12 h-12 bg-emerald-50 text-emerald-600 rounded-lg flex items-center justify-center">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2m-3 7h3m-3 4h3m-6-4h.01M9 16h.01"/></svg>
            </div>
        </div>
    </div>

    <!-- Charts Grid -->
    <div class="grid grid-cols-1 lg:grid-cols-3 gap-6">
        <!-- Main Chart -->
        <div class="lg:col-span-2 bg-white rounded-xl border border-slate-200 shadow-sm p-6">
            <div class="flex items-center justify-between mb-6">
                <div>
                    <h3 class="text-lg font-semibold text-slate-900">Monthly Performance</h3>
                </div>
                <div class="flex space-x-2 bg-slate-100 p-1 rounded-lg">
                    <button class="px-4 py-1.5 rounded-md text-xs font-semibold transition-all @(chartName == "BasicColumnChart" ? "bg-white text-slate-900 shadow-sm" : "text-slate-500 hover:text-slate-700")" @onclick='() => SetChart("BasicColumnChart")'>Columns</button>
                    <button class="px-4 py-1.5 rounded-md text-xs font-semibold transition-all @(chartName == "FiveYearLineChart" ? "bg-white text-slate-900 shadow-sm" : "text-slate-500 hover:text-slate-700")" @onclick='() => SetChart("FiveYearLineChart")'>Trend</button>
                </div>
            </div>
            
            <div class="h-[360px] w-full">
                @if (chartName == "BasicColumnChart" && Series is not null)
                {
                    <PageBasicColumnChart Id="BasicColumnChart" Title="" Series="Series" />
                }
                else if (chartName == "FiveYearLineChart" && FiveYearsLineChartData is not null)
                {
                    <PageFiveYearsLineChart Id="FiveYearLineChart" Title="" FiveYearsLineChartData="FiveYearsLineChartData" />
                }
                else
                {
                    <div class="flex items-center justify-center h-full text-slate-400 text-sm">
                        Loading chart data...
                    </div>
                }
            </div>
        </div>

        <!-- Top Products Widget -->
        <div class="lg:col-span-1 bg-white rounded-xl border border-slate-200 shadow-sm p-6 flex flex-col">
            <div class="mb-6">
                <h3 class="text-lg font-semibold text-slate-900">Top Items</h3>
            </div>
            @if (SixMostSoldProducts is not null)
            {
                <div class="space-y-4 flex-1">
                    @foreach (var prod in SixMostSoldProducts.Take(5))
                    {
                        <div class="flex items-center justify-between">
                            <div class="flex items-center space-x-3">
                                <div class="w-10 h-10 bg-slate-50 border border-slate-100 text-slate-600 rounded-full flex items-center justify-center text-sm font-semibold">
                                    @prod.name?.Substring(0, 1)
                                </div>
                                <div>
                                    <div class="text-sm font-medium text-slate-900">@prod.name</div>
                                </div>
                            </div>
                            <div class="text-sm font-semibold text-slate-700">@prod.data.FirstOrDefault()</div>
                        </div>
                    }
                </div>
            }
            else
            {
                <div class="flex items-center justify-center h-full text-slate-400 text-sm">Loading items...</div>
            }
        </div>
    </div>

    <!-- Secondary Charts Section -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6">
             <div class="mb-6">
                <h3 class="text-lg font-semibold text-slate-900">Revenue Trend</h3>
             </div>
             @if (MonthlyRevenueReportForThreeYears is not null)
             {
                 <div class="h-[300px]">
                     <PageMonthlyRevenueReportForThreeYearsChart Id="ThreeYearRevenue" Title="" MonthlyRevenueReportForThreeYears="MonthlyRevenueReportForThreeYears" />
                 </div>
             }
        </div>
        <div class="bg-white rounded-xl border border-slate-200 shadow-sm p-6">
             <div class="mb-6">
                <h3 class="text-lg font-semibold text-slate-900">Weekly Summary</h3>
             </div>
             @if (PastSevenDaysData is not null)
             {
                 <div class="h-[300px]">
                     <PagePastSevenDaysChart Id="WeeklyChart" Title="" PastSevenDaysData="PastSevenDaysData" />
                 </div>
             }
        </div>
    </div>
</div>

@code {
    private List<ProductInfo>? Series;
    private QtyOfTopFiveProductsByYearModel? BarBasicData;
    private List<MaxMinQtyOfProductsModel>? MaxMinQtyOfProducts;
    private List<ProductCategoryChartModel>? ProdAndProdCategory;
    private List<MonthlyRevenueReportResponseModel>? MonthlyRevenueReportForThreeYears;
    private List<SixMostSoldProductsModel>? SixMostSoldProducts;
    private PastSevenDaysModel? PastSevenDaysData;
    private FiveYearLineChart? FiveYearsLineChartData;
    private DonutChartResponseModel? PastFiveYearData;
    private DataReturnInfo? PastFiveYearFunnelData;
    private YearlySaleAmountModel? YearlySaleAmountData;
    private List<PastFiveYearsMonthlyModel>? PastFiveYearsMonthlyData;
    private PastFiveYearsDailyModel? PastFiveYearsDailyData;

    private string chartName = "BasicColumnChart";
    private int GrandTotal = 0;
    private int ProductCount = 0;
    private int CategoryCount = 0;
    private int VoucherCount = 0;

    protected override void OnInitialized()
    {
        StateContainer.OnChange += StateHasChanged;
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await LoadData();
        }
    }

    private async Task LoadData()
    {
        await db.GenerateDataByMonth();
        
        var products = await db.GetProductList();
        ProductCount = products?.Count ?? 0;
        
        var categories = await db.GetProductCategoryList();
        CategoryCount = categories?.Count ?? 0;
        
        var vouchers = await db.GetSaleVoucherHead();
        VoucherCount = vouchers?.Count ?? 0;
        
        GrandTotal = await db.GetGrandTotal();

        Series = await db.CurrentYearTopFiveProductsByMonth();
        BarBasicData = await db.QtyOfTopFiveProductsByYear();
        MaxMinQtyOfProducts = await db.MaxMinQtyOfProducts();
        ProdAndProdCategory = await db.ProductCategoryAndProduct();
        MonthlyRevenueReportForThreeYears = await db.MonthlyRevenueReportForThreeYear();
        SixMostSoldProducts = await db.SixMostSoldProducts();
        PastSevenDaysData = await db.PastSevenDays();
        FiveYearsLineChartData = await db.FiveYearLineChart();
        YearlySaleAmountData = await db.YearlySaleAmount();
        PastFiveYearData = await db.PastFiveYear(DateTime.Now);
        PastFiveYearFunnelData = await db.PastFiveYearFunnelChart(DateTime.Now);
        PastFiveYearsMonthlyData = await db.PastFiveYearMonthlySaleAmount(DateTime.Now);
        PastFiveYearsDailyData = await db.PastFiveYearsDailyAmount(DateTime.Now);

        StateHasChanged();
    }

    private void SetChart(string name)
    {
        chartName = name;
    }

    public void Dispose()
    {
        StateContainer.OnChange -= StateHasChanged;
    }
}